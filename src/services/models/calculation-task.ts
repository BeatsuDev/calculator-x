/**
 * 
 * @export
 * @interface CalculationTask
 */
export interface CalculationTask {
    /** 
     * A Calculation Task sent to the server.
     * 
     * @type {string}
     * @memberof CalculationTask
     */
    task: string;
}
